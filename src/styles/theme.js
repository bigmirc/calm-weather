import { extendTheme } from '@chakra-ui/react';

export const myTheme = extendTheme({
  colors: {
    primary: '#00B295',
    secondary: '#03F7EB',
    highlight: '#C9DAEA',
    black: '#191516',
  },
});
